<div class="card my-2">
    @if (ChildContent is not null)
    {
        @(ChildContent)
    }
    else
    {
        @: در این کامپوننت باید از کامپوننت های کارد هدر و کارد بادی و کارد فوتر استفاده کنید
    }
</div>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
}