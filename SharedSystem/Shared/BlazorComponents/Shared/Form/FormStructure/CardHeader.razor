<div class="card-header text-center border-2 border-bottom @(UpdateMode == true ? "border-warning" : "border-primary")">
    @if (ChildContent is not null)
    {
        @(ChildContent)
    }
    else
    {
        @(Resources.DataDictionary.SubmitForm)
        @* if (UpdateMode == true) *@
        @* { *@
        @*     @: - مود ویرایش *@
        @* } *@
    }
</div>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public bool UpdateMode { get; set; }
}