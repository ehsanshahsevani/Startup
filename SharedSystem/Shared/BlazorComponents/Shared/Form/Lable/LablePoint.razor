<i style="font-size: 5px" class="bi bi-circle-fill @(Utility.GetForgroundColorByBootstrapColor(Color))"></i>

@code {
    [Parameter] public BootstrapColor Color { get; set; } = BootstrapColor.Danger;
}